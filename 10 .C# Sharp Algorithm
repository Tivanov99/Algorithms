using System;

namespace Opp_Train
{
    public class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Concat(37));
        }
        public static bool Concat(int input)
        {
            return input % 3 == 0 || input % 7 == 0;
        }
    }
}
