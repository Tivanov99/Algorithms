using System;

namespace Opp_Train
{
    public class Program
    {
        static void Main(string[] args)
        {

            Console.WriteLine(Sum(3,2));
        }
        public static int Sum(int f, int s)
        {
            return f == s ? (f+s)*3 : f + s;
        }
    }
}
