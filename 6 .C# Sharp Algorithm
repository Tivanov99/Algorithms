using System;

namespace Opp_Train
{
    public class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine(Difference("Python",4));
        }
        public static string Difference(string x, int index)
        {
            return x.Remove(index, 1);
        }
    }
}
